{
  "name": "MagicKit Flutter",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "widgetbook",
      "command": "flutter run -d web-server --web-port 8090 --web-hostname 0.0.0.0",
      "description": "Serves the MagicKit example widgetbook app over HTTP on port 8090. Open http://localhost:8090 in Chrome to browse the UI component gallery."
    }
  ],
  "ports": [
    {
      "name": "widgetbook",
      "port": 8090
    }
  ]
}
