name: magickit_example
description: MagicKit UI Kit example app.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  magickit:
    path: ../packages/magickit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
